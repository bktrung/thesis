% !TEX root = ../main.tex
\chapter{Phương pháp xây dựng Vietnamese NeoBERT}
\label{chap:method}

Chương này trình bày chi tiết phương pháp xây dựng mô hình Vietnamese NeoBERT
(ViNeoBERT). Trước hết, chúng tôi mô tả tổng quan quy trình ba giai đoạn cùng các mô hình
nguồn được sử dụng (Mục~\ref{sec:method-overview}). Tiếp theo, chúng tôi giải thích lý do
lựa chọn ViDeBERTa làm mô hình hiến tặng và so sánh bộ tách từ với PhoBERT
(Mục~\ref{sec:donor-tokenizer}), rồi trình bày bước cắt tỉa và đồng bộ từ vựng
(Mục~\ref{sec:vocab}), quá trình trích xuất tập neo -- được mở rộng bằng các cặp dịch, một
cải tiến của đề tài so với SALT gốc (Mục~\ref{sec:anchor}), và phép chiếu SALT áp dụng
cho cả lớp nhúng đầu vào lẫn lớp đầu ra tách rời của NeoBERT (Mục~\ref{sec:salt-proj}).
Cuối cùng, chúng tôi mô tả giai đoạn căn chỉnh đóng băng (Mục~\ref{sec:freeze}) và giai
đoạn tiếp tục tiền huấn luyện thích nghi (Mục~\ref{sec:cpt}). Các giá trị siêu tham số cụ
thể được trình bày trong Chương~\ref{chap:experiment}.

% ======================================================================
\section{Tổng quan phương pháp}
\label{sec:method-overview}

\subsection{Đặt vấn đề}
\label{subsec:problem}

Như đã phân tích trong Chương~\ref{chap:related}, việc xây dựng một Encoder hiện đại cho
tiếng Việt từ NeoBERT đối mặt đồng thời ba thách thức: (1)~NeoBERT chỉ được tiền huấn luyện
trên ngữ liệu tiếng Anh, nên toàn bộ lớp nhúng của nó phản ánh phân phối từ vựng Anh ngữ;
(2)~tiếp tục tiền huấn luyện với một lớp nhúng \textit{khởi tạo ngẫu nhiên} dễ dẫn tới hiện
tượng quên lãng thảm họa; và (3)~huấn luyện lại từ đầu trên quy mô hàng nghìn tỷ token là
bất khả thi về tài nguyên. Phương pháp của đề tài giải quyết đồng thời ba thách thức này
bằng cách \textit{khởi tạo có thông tin} lớp nhúng tiếng Việt thông qua kỹ thuật SALT, sau
đó tinh chỉnh mô hình trên ngữ liệu tiếng Việt với chi phí khiêm tốn.

\subsection{Mô hình nguồn và mô hình hiến tặng}
\label{subsec:sources}

Phương pháp sử dụng hai mô hình tiền huấn luyện:
\begin{itemize}
  \item \textbf{NeoBERT}~\cite{lebreton2025neobert} đóng vai trò \textit{kiến trúc đích}:
        toàn bộ phần thân Transformer (28 lớp với RoPE, SwiGLU, Pre-RMSNorm) cùng không gian
        biểu diễn tiếng Anh được giữ nguyên và dùng làm hệ tọa độ đích cho phép chiếu. Một
        đặc điểm quan trọng là NeoBERT sử dụng \textit{đầu giải mã tách rời} (untied head):
        lớp đầu ra của tác vụ MLM là một ánh xạ tuyến tính riêng $\,y = W_{\mathrm{dec}}\,h
        + b_{\mathrm{dec}}\,$ với ma trận trọng số $W_{\mathrm{dec}}$ và véc-tơ độ chệch
        $b_{\mathrm{dec}}$ \textit{độc lập} với ma trận nhúng đầu vào $E$. Do đó, việc thích
        nghi sang tiếng Việt đòi hỏi khởi tạo \textit{cả hai} ma trận token: ma trận nhúng
        đầu vào $E$ và ma trận đầu ra $W_{\mathrm{dec}}$.
  \item \textbf{ViDeBERTa}~\cite{lai2023videberta} (bản \texttt{base}) đóng vai trò
        \textit{mô hình hiến tặng} (donor): cung cấp không gian nhúng tiếng Việt chất lượng
        cao làm nguồn để tái sử dụng. ViDeBERTa dùng bộ tách từ SentencePiece theo thuật
        toán Unigram với kích thước từ vựng lớn (khoảng 128 nghìn token), trong đó mỗi đơn
        vị từ vựng đi kèm một điểm log-xác suất.
\end{itemize}

\subsection{Quy trình ba giai đoạn}
\label{subsec:pipeline}

Mô hình ViNeoBERT được xây dựng qua ba giai đoạn nối tiếp, minh họa trong
Hình~\ref{fig:pipeline}:
\begin{enumerate}
  \item \textbf{Khởi tạo SALT}: xây dựng tập từ vựng tiếng Việt, trích xuất tập neo, rồi
        chiếu không gian nhúng của ViDeBERTa sang không gian NeoBERT để khởi tạo cả ma trận
        nhúng đầu vào lẫn ma trận đầu ra; độ chệch của đầu ra được khởi tạo theo tần suất từ
        đơn (Mục~\ref{sec:vocab}--\ref{sec:salt-proj}). Giai đoạn này \textit{không} cập
        nhật phần thân Transformer.
  \item \textbf{Căn chỉnh đóng băng}: đóng băng toàn bộ phần thân, chỉ huấn luyện ma trận
        nhúng và ma trận đầu ra trên một lượng nhỏ ngữ liệu tiếng Việt, nhằm để hai ma trận
        vừa khởi tạo căn chỉnh với nhau và với phần thân (Mục~\ref{sec:freeze}).
  \item \textbf{Tiếp tục tiền huấn luyện thích nghi}: tinh chỉnh toàn bộ mô hình trên ngữ
        liệu tiếng Việt quy mô lớn với mục tiêu MLM theo lịch tốc độ học WSD
        (Mục~\ref{sec:cpt}).
\end{enumerate}

\begin{figure}[ht]
  \centering
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
    node distance=7mm and 10mm,
    stage/.style={draw, rounded corners, minimum width=34mm, minimum height=13mm, align=center, font=\small, fill=blue!7},
    src/.style={draw, rounded corners, minimum width=30mm, minimum height=9mm, align=center, font=\footnotesize, fill=gray!10},
    >={Latex[length=2mm]}
  ]
    \node[src] (neo)  {NeoBERT\\(architecture + target space)};
    \node[src, below=of neo] (vide) {ViDeBERTa\\(donor model)};
    \coordinate (srcmid) at ($(neo.east)!0.5!(vide.east)$);
    \node[stage, anchor=west] (s1) at ($(srcmid)+(12mm,0)$) {Stage 1\\SALT initialization};
    \node[stage, right=of s1] (s2) {Stage 2\\Frozen alignment};
    \node[stage, right=of s2] (s3) {Stage 3\\CPT (WSD)};
    \node[src, right=of s3] (out) {ViNeoBERT};
    \draw[->] (neo.east)  -- (s1.west);
    \draw[->] (vide.east) -- (s1.west);
    \draw[->] (s1) -- (s2); \draw[->] (s2) -- (s3); \draw[->] (s3) -- (out);
    \node[font=\scriptsize, below=1mm of s1] {frozen body};
    \node[font=\scriptsize, below=1mm of s2] {frozen body};
    \node[font=\scriptsize, below=1mm of s3] {all parameters};
  \end{tikzpicture}%
  }
  \caption{Quy trình ba giai đoạn xây dựng ViNeoBERT.}
  \label{fig:pipeline}
\end{figure}

% ======================================================================
\section{Lựa chọn mô hình hiến tặng và bộ tách từ}
\label{sec:donor-tokenizer}

\subsection{Vai trò của bộ tách từ trong mô hình ngôn ngữ}
\label{subsec:tokenizer-intro}

Bộ tách từ (tokenizer) là thành phần đầu vào không thể tách rời của mô hình ngôn ngữ:
nó quyết định cách văn bản thô được phân đoạn thành các đơn vị (token) trước khi đưa vào
mô hình. Chất lượng của bộ tách từ ảnh hưởng trực tiếp tới (1)~độ hiệu quả biểu diễn --
một bộ tách từ tốt cho tiếng Việt sẽ tạo ra các token mang nghĩa rõ ràng, còn bộ tách
từ kém có thể cắt nhầm ranh giới hình vị; (2)~độ bao phủ từ vựng -- từ vựng không đủ lớn
buộc mô hình phải dùng nhiều token để biểu diễn một khái niệm đơn giản; và (3)~tính tương
thích khi chuyển giao -- bộ tách từ của mô hình hiến tặng phải đủ phong phú để cung cấp
neo ngữ nghĩa chất lượng cao.

Trong lĩnh vực xử lý ngôn ngữ tự nhiên, hai thuật toán tách từ phổ biến nhất cho tiếng
Việt là BPE (Byte-Pair Encoding) và Unigram SentencePiece. BPE được sử dụng bởi PhoBERT:
bắt đầu từ ký tự đơn, lặp lại gộp hai ký hiệu liền kề phổ biến nhất cho đến khi đạt kích
thước từ vựng mong muốn. Unigram SentencePiece (được ViDeBERTa sử dụng) tiếp cận ngược:
bắt đầu với một từ vựng lớn, loại dần các đơn vị ít thông tin nhất theo tiêu chí xác suất
Unigram; kết quả là một phân phối xác suất trên các cách phân đoạn, mỗi token được gán một
điểm log-xác suất.

\subsection{So sánh PhoBERT và ViDeBERTa làm mô hình hiến tặng}
\label{subsec:donor-compare}

Đáng chú ý, nghiên cứu gốc SALT~\cite{lee2025salt} sử dụng chính PhoBERT làm mô hình hiến
tặng tiếng Việt. Trong đề tài này, chúng tôi chủ đích chọn ViDeBERTa thay cho PhoBERT vì ba
lý do chính:

\textbf{1. Bộ tách từ tự chứa, không ràng buộc công cụ tách từ ngoài.} PhoBERT dùng
thuật toán BPE huấn luyện trên văn bản đã được \textit{tách từ sẵn} bằng
VnCoreNLP~\cite{vu2018vncorenlp} (``học\_sinh đi học\_kỳ này''); do đó mọi văn bản đưa
vào PhoBERT đều phải qua bước tách từ ngoài này, và một mô hình kế thừa từ vựng PhoBERT
sẽ mang theo ràng buộc đó cùng nguy cơ lệch phân phối khi công cụ tách từ hoạt động sai.
Ngược lại, ViDeBERTa dùng bộ tách từ SentencePiece Unigram thao tác ở cấp \textit{tiểu
từ} (sub-word) trực tiếp trên chuỗi Unicode; ViNeoBERT kế thừa bộ tách từ này nên xử lý
được văn bản thô của CulturaX -- kho dữ liệu dùng trong CPT của đề tài -- mà không phụ
thuộc vào một công cụ tách từ riêng.

\textbf{2. Từ vựng lớn hơn -- neo phong phú hơn.} ViDeBERTa có từ vựng $\approx$128.000
token (so với 64.000 của PhoBERT), trong đó mỗi đơn vị kèm điểm log-xác suất Unigram.
Từ vựng lớn hơn đồng nghĩa với nhiều ứng viên neo hơn trong bước SALT -- đặc biệt quan
trọng cho việc bao phủ từ vựng thuần Việt và từ ghép. Bảng~\ref{tab:tokenizer-compare}
tóm tắt sự khác biệt chính.

\textbf{3. Không gian nhúng phong phú về ngữ nghĩa tiếng Việt.} ViDeBERTa được tiền huấn
luyện theo kiến trúc DeBERTaV3~\cite{he2021deberta} trên kho ngữ liệu web CC100 tiếng Việt
quy mô lớn (khoảng 138GB văn bản)~\cite{lai2023videberta} -- lớn hơn nhiều lần so với 20GB
(Wikipedia + tin tức) của PhoBERT. Không gian nhúng học từ dữ liệu web quy mô lớn giúp phép
chiếu SALT tạo ra các véc-tơ ban đầu tốt hơn, vì neo lấy từ ViDeBERTa sẽ mã hóa ngữ nghĩa
tiếng Việt chính xác hơn.

\begin{table}[ht]
  \caption{So sánh bộ tách từ của PhoBERT và ViDeBERTa.}
  \label{tab:tokenizer-compare}
  \centering
  \small
  \begin{tabular}{lll}
    \hline
    \textbf{Đặc điểm}             & \textbf{PhoBERT}           & \textbf{ViDeBERTa} \\
    \hline
    Thuật toán tách từ            & BPE                        & SentencePiece Unigram \\
    Kích thước từ vựng            & 64.000                     & $\approx$128.000 \\
    Đầu vào yêu cầu               & Đã tách từ (VnCoreNLP)     & Văn bản thô \\
    Điểm log-xác suất Unigram     & Không có                   & Có (dùng để cắt tỉa) \\
    Dữ liệu tiền huấn luyện       & 20GB (Wikipedia + tin tức) & $\approx$138GB (CC100) \\
    Tương thích CulturaX (raw)    & Không trực tiếp            & Có \\
    Tiềm năng neo SALT            & Hạn chế                    & Phong phú \\
    \hline
  \end{tabular}
\end{table}

% ======================================================================
\section{Cắt tỉa và đồng bộ từ vựng}
\label{sec:vocab}

Phép chiếu SALT yêu cầu lớp nhúng nguồn và đích có cùng kích thước từ vựng. Vì từ vựng của
ViDeBERTa lớn hơn nhiều so với 30.522 token của NeoBERT, cần một bước \textit{cắt tỉa từ
vựng} (vocabulary pruning) để đồng bộ kích thước. Quá trình này giữ lại toàn bộ các token
đặc biệt (như \texttt{[CLS]}, \texttt{[SEP]}, \texttt{[MASK]}, \texttt{[PAD]}), sau đó chọn
bổ sung các token thông thường có \textit{điểm log-xác suất Unigram cao nhất} -- tức các đơn
vị từ vựng phổ biến và đáng tin cậy nhất -- cho tới khi đạt đúng 30.522 token.

Gọi $\mathcal{V}_s$ là tập token đặc biệt và $\mathcal{V}_n$ là tập token thông thường của
ViDeBERTa, với điểm Unigram $\mathrm{score}(\cdot)$. Tập từ vựng tiếng Việt sau cắt tỉa
$\mathcal{V}_t$ được xác định như Công thức~\eqref{eq:prune}:
\begin{equation}
  \label{eq:prune}
  \mathcal{V}_t = \mathcal{V}_s \;\cup\; \mathrm{TopK}\big(\mathcal{V}_n,\, \mathrm{score},\,
  K\big), \qquad K = 30{,}522 - |\mathcal{V}_s|
\end{equation}
Trong đó $\mathrm{TopK}(\mathcal{V}_n, \mathrm{score}, K)$ trả về $K$ token có điểm Unigram
cao nhất. Bộ tách từ được ghi lại với ánh xạ chỉ số mới, đồng thời lưu một ánh xạ
$\pi:\, \text{id mới} \rightarrow \text{id gốc trong ViDeBERTa}$ để truy xuất đúng hàng nhúng
hiến tặng ở các bước sau. Phép so khớp chuỗi piece là chính xác vì các đơn vị Unigram là duy
nhất trong từ vựng.

% ======================================================================
\section{Trích xuất tập neo (anchor set)}
\label{sec:anchor}

\subsection{Vai trò của tập neo}
\label{subsec:anchor-role}

\textit{Tập neo} (anchor set) $\mathcal{A}$ là tập các cặp token $(t_{\mathrm{vi}},
t_{\mathrm{neo}})$ có tương ứng ngữ nghĩa rõ ràng giữa từ vựng tiếng Việt (ViDeBERTa đã cắt
tỉa) và từ vựng NeoBERT. Mỗi cặp neo cung cấp một điểm \textit{đã biết} của ánh xạ giữa hai
không gian nhúng, và giữ vai trò kép trong phép chiếu SALT:
\begin{itemize}
  \item \textbf{Khởi tạo trực tiếp}: bản thân token neo $t_{\mathrm{vi}}$ được khởi tạo bằng
        cách \textit{sao chép nguyên vẹn} hàng nhúng NeoBERT của $t_{\mathrm{neo}}$, bảo đảm
        độ trung thực tuyệt đối với không gian đích cho các token đã có tương ứng chắc chắn.
  \item \textbf{Tập giám sát}: các cặp $(e_{\mathrm{vi}}, e_{\mathrm{neo}})$ của tập neo đóng
        vai trò tập giám sát (support set) để \textit{học các ánh xạ tuyến tính cục bộ}, từ đó
        suy ra nhúng cho những token \textit{không} thuộc tập neo (Mục~\ref{sec:salt-proj}).
\end{itemize}
Do đó, chất lượng và độ phủ của tập neo quyết định trực tiếp chất lượng của phép chiếu SALT.

Trong nghiên cứu gốc, SALT~\cite{lee2025salt} cũng như FOCUS~\cite{dobler2023focus} chỉ khai
thác các token \textit{trùng lặp bề mặt} (cùng dạng chuỗi) giữa hai từ vựng làm neo. Đối với
cặp ngôn ngữ Anh--Việt vốn khác biệt lớn về hình thái, số token trùng lặp bề mặt là khá hạn
chế và độ phủ ngữ nghĩa hẹp. Vì vậy, chúng tôi xây dựng tập neo từ \textit{ba nguồn bổ sung
cho nhau}: (1)~neo trùng lặp bề mặt \textit{đã xác minh nghĩa}, (2)~neo số, và (3)~các
\textit{cặp dịch} Anh--Việt khai thác tự động. Trong đó, việc mở rộng tập neo bằng cặp dịch là
một trong những đóng góp chính của đề tài.

\subsection{Neo trùng lặp bề mặt và neo số}
\label{subsec:anchor-surface}

\textbf{Neo trùng lặp bề mặt.} Theo ý tưởng gốc của SALT, các token có \textit{cùng dạng
chuỗi} ở cả hai từ vựng là ứng viên neo tự nhiên, bởi trùng dạng chuỗi thường hàm ý cùng khái
niệm -- điển hình là từ vay mượn, tên riêng và thuật ngữ khoa học (\textit{internet, vitamin,
radio}). Chúng tôi trích các \textit{từ đầy đủ} (word-initial), độ dài đủ lớn, xuất hiện ở cả
hai từ vựng và lấy giao làm tập ứng viên.

Tuy nhiên, vì tiếng Việt cũng dùng hệ chữ Latin như tiếng Anh, \textit{cùng dạng chuỗi chưa
chắc cùng nghĩa}: nhiều token trùng chuỗi thực chất là hiện tượng \textit{đồng tự khác nghĩa}
(false friends). Chẳng hạn ``song'' trong tiếng Việt nghĩa là \textit{song song} hoặc liên từ
\textit{nhưng}, còn ``song'' trong tiếng Anh nghĩa là \textit{bài hát}; ``sang'' tiếng Việt
nghĩa là \textit{sang trọng}, còn trong tiếng Anh là dạng quá khứ của \textit{sing}. Một cặp
neo đồng tự khác nghĩa sẽ tạo ra một điểm ánh xạ sai lệch, kéo phép chiếu đi chệch. Để loại
các neo giả này, chúng tôi \textit{xác minh nghĩa} từng ứng viên bằng dịch máy MarianMT
Việt--Anh~\cite{junczys2018marian}: chỉ giữ lại làm neo khi bản dịch tiếng Anh \textit{trùng
khớp chính chuỗi gốc}. Một khái niệm chung thực sự (vay mượn, tên riêng) sẽ dịch về chính nó
và được giữ; ngược lại, một token đồng tự khác nghĩa sẽ dịch sang một từ tiếng Anh khác và bị
loại bỏ.

\textbf{Neo số.} Song song, chúng tôi giữ trực tiếp các token \textit{số} xuất hiện ở cả hai
từ vựng làm neo mà \textit{không} cần xác minh nghĩa, vì ký hiệu số mang tính phổ quát
(universal) giữa các ngôn ngữ -- ``2024'' biểu thị cùng một giá trị bất kể ngôn ngữ. Đây là
nguồn neo tin cậy tuyệt đối và không phụ thuộc chất lượng dịch máy.

\subsection{Mở rộng tập neo bằng cặp dịch ba tầng}
\label{subsec:anchor-mining}

Hai nguồn trên cho số neo hạn chế và tập trung ở từ vay mượn, tên riêng và số -- không bao phủ
được phần lớn kho từ vựng thuần Việt. Để \textit{làm giàu và mở rộng độ phủ} của tập neo,
chúng tôi bổ sung nguồn neo thứ ba: các cặp dịch Anh--Việt khai thác tự động, trong đó
$t_{\mathrm{vi}}$ và $t_{\mathrm{neo}}$ \textit{khác dạng chuỗi} nhưng tương ứng về nghĩa (ví
dụ ``nước'' $\leftrightarrow$ ``water''). Đây là đóng góp chính của đề tài ở bước khởi tạo.

Trước khi khai thác, chúng tôi lọc để chỉ giữ các \textit{từ tiếng Việt đầy đủ và hợp lệ}: loại
token chứa ký tự không bản địa (\textit{f, j, w, z}), giới hạn độ dài, yêu cầu đúng một cụm
nguyên âm liền mạch, và tuân thủ các quy tắc chính tả tiếng Việt (chẳng hạn \textit{k, gh,
ngh} chỉ đứng trước \textit{i, e, ê, y}; \textit{q} luôn đi kèm \textit{u}). Bộ lọc chính tả
này bảo đảm ứng viên là từ tiếng Việt thực thụ, tránh nhiễu từ các chuỗi ngoại lai. Trên tập
đã lọc, cặp dịch được khai thác theo \textit{ba tầng} bổ sung cho nhau:
\begin{itemize}
  \item \textbf{Tầng 1 -- dịch ngược (back-translation)}: mỗi từ tiếng Việt được dịch sang
        tiếng Anh rồi \textit{dịch ngược} trở lại tiếng Việt; cặp neo chỉ được chấp nhận khi
        (a)~kết quả dịch ngược trùng với từ gốc, (b)~bản dịch tiếng Anh là một \textit{từ đơn}
        khác với từ gốc, và (c)~thuộc từ vựng NeoBERT.
  \item \textbf{Tầng 2 -- từ ghép}: các từ ghép tiếng Việt (đa âm tiết) chưa được phủ được
        dịch trực tiếp sang tiếng Anh, giữ lại khi bản dịch là một từ đơn thuộc từ vựng
        NeoBERT.
  \item \textbf{Tầng 3 -- từ đơn còn lại}: các từ đơn chưa được phủ, sau khi loại bỏ một danh
        sách từ chức năng và từ dễ nhập nhằng (blacklist), được dịch trực tiếp và giữ theo
        cùng ràng buộc ``một từ đơn thuộc từ vựng NeoBERT''; tầng này được giới hạn số lượng để
        kiểm soát chi phí và chất lượng.
\end{itemize}

Cơ sở khoa học của phép kiểm tra \textit{dịch ngược} ở Tầng 1 là: một từ đa nghĩa thường không
dịch ngược về chính nó một cách ổn định, do bản dịch trung gian có thể ứng với một nghĩa khác.
Vì vậy, ràng buộc dịch ngược trùng khớp tự nhiên loại bỏ phần lớn các từ đa nghĩa, chỉ giữ lại
các cặp tương ứng một--một đáng tin cậy. Ràng buộc ``bản dịch là một từ đơn thuộc từ vựng
NeoBERT'' bảo đảm mỗi cặp neo ứng đúng \textit{một} token NeoBERT.

Tập neo cuối cùng là \textit{hợp} của ba nguồn -- neo bề mặt đã xác minh, neo số và cặp dịch ba
tầng -- sau khi loại các cặp trùng lặp, thu được ánh xạ $t_{\mathrm{vi}} \mapsto
t_{\mathrm{neo}}$ dùng thống nhất cho cả lớp nhúng đầu vào lẫn lớp đầu ra ở
Mục~\ref{sec:salt-proj}. Thực nghiệm khởi tạo cho thấy việc mở rộng tập neo bằng cặp dịch làm
tăng đáng kể số lượng neo và độ phủ ngữ nghĩa, qua đó cải thiện chất lượng khởi tạo so với
phương án chỉ dùng neo trùng lặp bề mặt như nghiên cứu gốc.

Để minh họa cụ thể ba nguồn neo, xét bốn token tiếng Việt đại diện. Token \textit{internet}
trùng dạng chuỗi ở cả hai từ vựng và dịch máy trả về đúng ``internet'', nên được giữ làm
\textbf{neo bề mặt} và khởi tạo bằng chính hàng nhúng NeoBERT của ``internet''. Token số
\textit{2024} xuất hiện ở cả hai từ vựng và được nhận trực tiếp làm \textbf{neo số} mà không
cần qua dịch. Token thuần Việt \textit{nước} khác hẳn dạng chuỗi so với ``water'': ở Tầng~1,
dịch máy cho ``water'', dịch ngược cho lại ``nước'', và ``water'' là một từ đơn thuộc từ
vựng NeoBERT -- cả ba điều kiện thỏa mãn nên cặp $(\textit{nước},\ \textit{water})$ được thêm
làm \textbf{cặp dịch}. Ngược lại, token đồng tự khác nghĩa ``song'' (tiếng Việt: \textit{song
song}) dịch sang ``parallel'' chứ không phải ``song'', nên bị loại khỏi tập neo bề mặt.
Thuật toán~\ref{alg:anchor} tóm tắt toàn bộ quy trình trích xuất tập neo từ ba nguồn.

\begin{algorithm}[ht]
  \caption{Trích xuất tập neo từ ba nguồn}
  \label{alg:anchor}
  \begin{algorithmic}[1]
    \Require Từ vựng tiếng Việt $\mathcal{V}_t$, từ vựng NeoBERT $\mathcal{V}_{\mathrm{neo}}$;
      dịch máy $\mathrm{MT}$; bộ lọc chính tả tiếng Việt $\mathrm{vi?}(\cdot)$
    \Ensure Tập neo $\mathcal{A}$ dạng ánh xạ $t_{\mathrm{vi}} \mapsto t_{\mathrm{neo}}$
    \State $\mathcal{A} \gets \emptyset$
    \ForAll{từ đầy đủ $t \in \mathcal{V}_t \cap \mathcal{V}_{\mathrm{neo}}$}
      \Comment{(A) neo bề mặt}
      \If{$\mathrm{MT}(t) = t$}
        \Comment{loại đồng tự khác nghĩa}
        \State $\mathcal{A} \gets \mathcal{A} \cup \{(t, t)\}$
      \EndIf
    \EndFor
    \ForAll{token số $t \in \mathcal{V}_t \cap \mathcal{V}_{\mathrm{neo}}$}
      \Comment{(B) neo số}
      \State $\mathcal{A} \gets \mathcal{A} \cup \{(t, t)\}$
    \EndFor
    \ForAll{từ Việt hợp lệ $t \in \mathcal{V}_t$ thỏa $\mathrm{vi?}(t)$}
      \Comment{(C) cặp dịch}
      \State $e \gets \mathrm{MT}(t)$
      \If{$e$ là từ đơn \textbf{và} $e \in \mathcal{V}_{\mathrm{neo}}$ \textbf{và} $\mathrm{MT}^{-1}(e) = t$}
        \State $\mathcal{A} \gets \mathcal{A} \cup \{(t, e)\}$
        \Comment{dịch ngược trùng khớp}
      \EndIf
    \EndFor
    \State \Return $\mathcal{A}$ sau khi khử các cặp trùng lặp
  \end{algorithmic}
\end{algorithm}

% ======================================================================
\section{Phép chiếu SALT cho lớp nhúng và lớp đầu ra}
\label{sec:salt-proj}

Mục này trình bày phép chiếu SALT -- bước khởi tạo cốt lõi. Với mỗi token tiếng Việt
\textit{không thuộc} tập neo, chúng tôi xây dựng nhúng của nó bằng một ánh xạ tuyến tính cục
bộ học từ các neo lân cận về mặt ngữ nghĩa. Các token neo và token đặc biệt được sao chép
trực tiếp hàng tương ứng từ NeoBERT.

\subsection{Lựa chọn neo bằng Sparsemax}
\label{subsec:anchor-select}

Để xác định các neo liên quan tới một token đích $t$, chúng tôi đo độ tương đồng ngữ nghĩa
trong không gian nhúng từ tĩnh fastText~\cite{bojanowski2017fasttext, grave2018fasttext}. Gọi
$\mathbf{f}_t$ là véc-tơ fastText của $t$ và $\{\mathbf{f}_a\}_{a\in\mathcal{A}}$ là véc-tơ
fastText của các neo. Độ tương đồng cosine và trọng số neo được tính như
Công thức~\eqref{eq:sparsemax-sel}:
\begin{equation}
  \label{eq:sparsemax-sel}
  s_{t,a} = \frac{\mathbf{f}_t^\top \mathbf{f}_a}{\lVert \mathbf{f}_t\rVert\, \lVert \mathbf{f}_a\rVert},
  \qquad
  \mathbf{w}_t = \mathrm{sparsemax}\big(\mathbf{s}_t\big),
  \qquad
  \mathcal{S}_t = \{\, a : w_{t,a} > 0 \,\}
\end{equation}
Trong đó $\mathrm{sparsemax}(\cdot)$~\cite{martins2016sparsemax} là phép chiếu lên đơn hình
xác suất, cho kết quả \textit{thưa} -- chỉ một tập con nhỏ $\mathcal{S}_t$ các neo liên quan
nhất nhận trọng số khác 0. Việc dùng Sparsemax thay cho softmax có lý do thực tế cụ thể:
softmax luôn gán trọng số khác 0 cho \textit{tất cả} các neo, kể cả những neo ở rất xa token
đích về mặt ngữ nghĩa -- khiến ánh xạ tuyến tính cục bộ bị nhiễu bởi các điểm không liên
quan. Sparsemax tự động đặt trọng số bằng 0 cho các neo có độ tương đồng thấp; chỉ tập con
$\mathcal{S}_t$ thực sự gần nghĩa mới tham gia phép chiếu, giúp ma trận $X_t$ phản ánh đúng
quan hệ tuyến tính cục bộ quanh token $t$.

\subsection{Ánh xạ tuyến tính cục bộ theo từng token}
\label{subsec:local-map}

Như đã lập luận ở Mục~\ref{sec:geometry}, do hai không gian nhúng là bất đẳng hướng và không
đẳng cấu, một ánh xạ tuyến tính toàn cục duy nhất là không đủ. Thay vào đó, SALT học một
\textit{ánh xạ tuyến tính riêng cho từng token} từ tập neo được chọn. Gọi $A_t^{\mathrm{src}}
\in \mathbb{R}^{k\times h}$ là ma trận xếp chồng các nhúng \textit{hiến tặng} (ViDeBERTa) của
$k = |\mathcal{S}_t|$ neo trong $\mathcal{S}_t$, và $A_t^{\mathrm{tgt}} \in \mathbb{R}^{k\times
h}$ là ma trận các hàng \textit{đích} (NeoBERT) tương ứng. Khi số neo đủ lớn ($k \ge k_0$, chúng tôi chọn $k_0 = 8$ qua thực nghiệm -- giá trị nhỏ
hơn khiến giả nghịch đảo kém ổn định và chất lượng chiếu giảm đáng kể), ánh xạ cục bộ
$X_t$ và nhúng kết quả $\mathbf{e}_t$ được xác định như Công thức~\eqref{eq:local-map}:
\begin{equation}
  \label{eq:local-map}
  X_t = \big(A_t^{\mathrm{src}}\big)^{+} A_t^{\mathrm{tgt}}, \qquad
  \mathbf{e}_t = \mathbf{d}_t\, X_t
\end{equation}
Trong đó $(\cdot)^{+}$ là \textit{giả nghịch đảo} Moore--Penrose (nghiệm bình phương tối
thiểu chuẩn nhỏ nhất), $\mathbf{d}_t \in \mathbb{R}^{h}$ là nhúng hiến tặng của token $t$, và
$h$ là số chiều ẩn. Ánh xạ $X_t$ học quan hệ tuyến tính giữa hai không gian \textit{cục bộ
quanh token $t$}, dựa trên các neo gần nghĩa nhất với nó.

Khi số neo không đủ ($k < k_0$), giả nghịch đảo trở nên kém ổn định; khi đó chúng tôi thoái
lui về \textit{trung bình có trọng số Sparsemax} của các hàng đích, như
Công thức~\eqref{eq:wavg}:
\begin{equation}
  \label{eq:wavg}
  \mathbf{e}_t = \frac{\sum_{a\in\mathcal{S}_t} w_{t,a}\, \mathbf{a}^{\mathrm{tgt}}}
                      {\sum_{a\in\mathcal{S}_t} w_{t,a}}
\end{equation}
Trường hợp token không có véc-tơ fastText hợp lệ, nhúng được khởi tạo ngẫu nhiên theo phân
phối chuẩn khớp với thống kê của NeoBERT.

Toàn bộ phép chiếu SALT cho \textit{một} ma trận đích -- áp dụng lần lượt cho ma trận nhúng
đầu vào và ma trận đầu ra ở Mục~\ref{subsec:both-matrices} -- được tóm tắt trong
Thuật toán~\ref{alg:salt}.

\begin{algorithm}[ht]
  \caption{Phép chiếu SALT theo từng token cho một ma trận đích}
  \label{alg:salt}
  \begin{algorithmic}[1]
    \Require Từ vựng $\mathcal{V}_t$; tập neo $\mathcal{A}$; nhúng hiến tặng $\{\mathbf{d}_t\}$;
      hàng đích NeoBERT $\{\mathbf{a}^{\mathrm{tgt}}\}$; véc-tơ fastText $\{\mathbf{f}_t\}$;
      ngưỡng $k_0$; chuẩn trung bình $\mu_{\mathrm{neo}}$
    \Ensure Ma trận khởi tạo $E \in \mathbb{R}^{|\mathcal{V}_t| \times h}$
    \ForAll{token $t \in \mathcal{V}_t$}
      \If{$t$ là neo hoặc token đặc biệt}
        \State $E[t] \gets \mathbf{a}^{\mathrm{tgt}}_{t}$
        \Comment{sao chép nguyên hàng NeoBERT}
      \ElsIf{$t$ không có véc-tơ fastText}
        \State $E[t] \gets$ mẫu ngẫu nhiên khớp thống kê NeoBERT
      \Else
        \State $s_{t,a} \gets \cos(\mathbf{f}_t, \mathbf{f}_a)$ cho mọi $a \in \mathcal{A}$
        \State $\mathbf{w}_t \gets \mathrm{sparsemax}(\mathbf{s}_t)$;\quad
               $\mathcal{S}_t \gets \{a : w_{t,a} > 0\}$
        \If{$|\mathcal{S}_t| \ge k_0$}
          \State $X_t \gets (A_t^{\mathrm{src}})^{+} A_t^{\mathrm{tgt}}$;\quad
                 $E[t] \gets \mathbf{d}_t\, X_t$
          \Comment{ánh xạ cục bộ}
        \Else
          \State $E[t] \gets \big(\textstyle\sum_a w_{t,a}\,\mathbf{a}^{\mathrm{tgt}}\big)\big/\sum_a w_{t,a}$
          \Comment{trung bình có trọng số}
        \EndIf
        \State $E[t] \gets E[t] \cdot \mu_{\mathrm{neo}} / \lVert E[t] \rVert$
        \Comment{hiệu chỉnh chuẩn (ma trận nhúng)}
      \EndIf
    \EndFor
    \State \Return $E$
  \end{algorithmic}
\end{algorithm}

\subsection{Áp dụng cho cả lớp nhúng đầu vào và lớp đầu ra}
\label{subsec:both-matrices}

Đây là điểm khác biệt then chốt so với SALT gốc. Vì NeoBERT dùng đầu giải mã \textit{tách
rời}, ma trận đầu ra $W_{\mathrm{dec}}$ là một ma trận độc lập với hình học riêng -- và theo
kết quả về hiện tượng thoái hóa biểu diễn (Mục~\ref{sec:geometry}), nó \textit{không} thể
được khởi tạo bằng cách sao chép ma trận nhúng. Do đó, chúng tôi áp dụng \textit{cùng một quy
trình chiếu SALT theo từng token} cho cả hai ma trận, chỉ khác ở tập hàng đích:
\begin{itemize}
  \item Với \textbf{ma trận nhúng đầu vào} $E$: hàng đích $A_t^{\mathrm{tgt}}$ lấy từ các
        \textit{hàng nhúng} NeoBERT của các neo.
  \item Với \textbf{ma trận đầu ra} $W_{\mathrm{dec}}$: hàng đích $A_t^{\mathrm{tgt}}$ lấy từ
        các \textit{hàng đầu giải mã} NeoBERT của các neo.
\end{itemize}
Nhờ vậy, mỗi ma trận được khởi tạo trong đúng không gian của nó, phản ánh trung thực quan hệ
ngữ nghĩa mà các neo thiết lập trong từng không gian.

\subsection{Hiệu chỉnh chuẩn của lớp nhúng}
\label{subsec:norm-calib}

Như đã lưu ý ở Mục~\ref{subsec:rmsnorm}, NeoBERT dùng Pre-RMSNorm vốn chuẩn hóa theo độ lớn
của véc-tơ; do đó độ lớn (norm) của mỗi hàng nhúng ảnh hưởng trực tiếp đến hành vi mô hình.
Các nhúng sinh ra từ phép chiếu SALT thường có độ lớn lệch so với phân phối của NeoBERT. Để
tránh gây bất ổn định, chúng tôi hiệu chỉnh độ lớn của mỗi hàng \textit{không phải neo} về
độ lớn trung bình của NeoBERT trong khi giữ nguyên hướng, như Công thức~\eqref{eq:norm-calib}:
\begin{equation}
  \label{eq:norm-calib}
  \mathbf{e}_t \;\leftarrow\; \mathbf{e}_t \cdot \frac{\mu_{\mathrm{neo}}}{\lVert \mathbf{e}_t\rVert},
  \qquad
  \mu_{\mathrm{neo}} = \frac{1}{|\mathcal{V}_{\mathrm{neo}}|}\sum_{v} \lVert E^{\mathrm{neo}}_v\rVert
\end{equation}
Trong đó $\mu_{\mathrm{neo}}$ là độ lớn trung bình của các hàng nhúng NeoBERT. Các hàng neo
và token đặc biệt được giữ nguyên để bảo toàn độ trung thực với NeoBERT.

\subsection{Khởi tạo độ chệch đầu ra theo tần suất}
\label{subsec:freq-bias}

Lớp đầu ra của NeoBERT có một véc-tơ độ chệch $b_{\mathrm{dec}}$ mã hóa \textit{tiên nghiệm
tần suất} của token. Các phương pháp khởi tạo trước đây thường để độ chệch này bằng 0, khiến
mô hình ở bước đầu phải lãng phí công sức học lại phân phối tần suất biên. Đề tài khởi tạo
$b_{\mathrm{dec}}$ theo \textit{log-tần-suất từ đơn} (unigram log-frequency) của tiếng Việt
theo phương pháp của Meister và cộng sự~\cite{meister2022naturalbias}, như
Công thức~\eqref{eq:freq-bias}:
\begin{equation}
  \label{eq:freq-bias}
  b_{\mathrm{dec},v} = \log p_v, \qquad
  p_v = \frac{c_v + 1}{\sum_{u\in\mathcal{V}_t} c_u + |\mathcal{V}_t|}
\end{equation}
Trong đó $c_v$ là số lần xuất hiện của token $v$ trong ngữ liệu tiếng Việt (đếm qua 20.000
văn bản CulturaX-vi với bộ tách từ đã cắt tỉa), và phép cộng 1 là làm trơn Laplace. Cụ thể,
phép đếm sử dụng đúng cùng bộ tách từ sẽ dùng trong huấn luyện, bảo đảm chỉ số token trong
$b_{\mathrm{dec}}$ khớp với các hàng của ma trận nhúng và ma trận đầu ra.

Cơ sở lý thuyết của cách khởi tạo này đến từ một quan sát của Meister và cộng
sự~\cite{meister2022naturalbias}: lớp tuyến tính cuối phân rã tự nhiên thành \textit{tích của
hai phân phối} (product of experts):
\begin{equation}
  \label{eq:poe}
  p_\theta(y \mid y_{<t})
  \;\propto\;
  \underbrace{e^{W\phi(y_{<t})}}_{p_{W\phi}(\cdot\mid y_{<t})\;\text{(ngữ cảnh)}}
  \;\cdot\;
  \underbrace{e^{b}}_{p_b(\cdot)\;\text{(tiên nghiệm)}}
\end{equation}
Thành phần $p_b$ là phân phối vô điều kiện hoàn toàn độc lập với ngữ cảnh. Khi $b_{\mathrm{dec}}
= \log p_v$, thành phần này mã hóa phân phối từ đơn tiếng Việt, nhờ đó \textit{giải phóng} phần
còn lại $p_{W\phi}$ để chuyên biệt hóa vào thông tin ngữ nghĩa thay vì phải mô hình hóa lại tần
suất bề mặt. Điều này có thể diễn đạt chặt chẽ qua gradient hàm mất mát theo độ chệch:
\begin{equation}
  \label{eq:bias-grad}
  \frac{\partial\mathcal{L}}{\partial b_v}
  = \mathbb{E}[\operatorname{softmax}(y)_v] - \hat{p}_v
\end{equation}
tức hiệu giữa phân phối biên mô hình đang dự đoán và phân phối biên thực nghiệm của dữ liệu.
Khi $b = \log p_v$ và $\gamma$ đủ nhỏ, $\mathbb{E}[\operatorname{softmax}(y)_v] \approx p_v
\approx \hat{p}_v$, nên gradient của $b_v$ xấp xỉ bằng không ngay từ bước đầu: toàn bộ tín hiệu
gradient được dồn vào $W_{\mathrm{dec}}$ và phần thân Transformer để học ngữ cảnh và ngữ
nghĩa, thay vì lãng phí vào việc hiệu chỉnh phân phối biên.
Meister và cộng sự xác nhận thực nghiệm rằng: (i)~ngay từ bước đầu huấn luyện, mô
hình ngôn ngữ đã ``thu hẹp'' đầu ra về phân phối từ đơn bất kể ngữ cảnh, và quá trình đó tốn
hàng trăm đến hàng nghìn bước gradient nếu khởi tạo $b = \mathbf{0}$; (ii)~khởi tạo theo
log-unigram cải thiện hiệu quả huấn luyện (diện tích dưới đường cong BLEU trên tập phát triển)
trên 5/6 cặp ngôn ngữ thử nghiệm, đồng thời cải thiện BLEU cuối trên cùng số cặp.

Nhờ cách khởi tạo này, hàm mất mát khởi điểm xấp xỉ \textit{entropy từ đơn} của tiếng Việt
(${\approx}7{,}3$ nat), thấp hơn đáng kể so với mức ngẫu nhiên $\log|\mathcal{V}_t|$
(${\approx}10{,}3$ nat với $|\mathcal{V}_t| = 30.522$). Ý tưởng khởi tạo độ chệch theo xác
suất tiên nghiệm để ổn định giai đoạn đầu huấn luyện cũng được Lin và cộng
sự~\cite{lin2017focal} áp dụng trong ngữ cảnh phát hiện đối tượng với mất cân bằng lớp nặng,
mặc dù bài toán đó dùng một hằng số vô hướng tiên nghiệm thay vì toàn bộ phân phối biên trên
từ vựng.

\subsection{Co trọng số đầu ra để tiên nghiệm dẫn dắt giai đoạn đầu}
\label{subsec:dec-scale}

Tiên nghiệm tần suất ở trên chỉ phát huy tác dụng nếu ở bước khởi đầu, thành phần logit do
trọng số $W_{\mathrm{dec}}$ sinh ra \textit{không lấn át} độ chệch $b_{\mathrm{dec}}$. Ngay
sau phép chiếu, các hàng của $W_{\mathrm{dec}}$ tuy đã mang cấu trúc ngữ nghĩa từ SALT nhưng
chưa từng được huấn luyện cùng phần thân trên tiếng Việt, nên biên độ logit của chúng chưa
đáng tin cậy: nếu giữ nguyên, các logit ``nhiễu'' này sẽ nhấn chìm tín hiệu tiên nghiệm. Đây là điểm
khác biệt then chốt so với huấn luyện từ đầu: trong ngữ cảnh đó, $W_{\mathrm{dec}}$ được khởi
tạo ngẫu nhiên với giá trị gần 0, nên điều kiện ``tiên nghiệm dẫn dắt'' thỏa mãn tự nhiên mà
không cần thêm can thiệp~\cite{meister2022naturalbias}. Trong chuyển giao ngôn ngữ chéo,
$W_{\mathrm{dec}}$ kế thừa biên độ lớn từ không gian NeoBERT tiếng Anh, khiến điều kiện đó
không còn tự nhiên thỏa mãn và phải được khôi phục thủ công -- đây là nhu cầu đặc thù của bài
toán chuyển giao lớp nhúng mà phương pháp đề xuất cần giải quyết. Vì
vậy, chúng tôi \textit{co} (scale) trọng số đầu ra bằng một hệ số vô hướng $\gamma$ như
Công thức~\eqref{eq:dec-scale}:
\begin{equation}
  \label{eq:dec-scale}
  y \;=\; \gamma\, W_{\mathrm{dec}}\, h + b_{\mathrm{dec}}, \qquad \gamma = 0{,}1
\end{equation}
Trong đó $h$ là biểu diễn ẩn ở đầu ra của phần thân và $\gamma$ là hệ số co áp dụng một lần
lên $W_{\mathrm{dec}}$ tại thời điểm khởi tạo. Cơ sở khoa học của lựa chọn này gồm hai vế:
\begin{itemize}
  \item \textbf{Gần 0 -- để tiên nghiệm dẫn dắt}: với $\gamma$ nhỏ, $\lVert \gamma\,
        W_{\mathrm{dec}} h \rVert$ trở nên nhỏ so với biên độ của $b_{\mathrm{dec}}$, nên
        phân phối dự đoán ban đầu do tiên nghiệm tần suất chi phối: mất mát khởi điểm xấp xỉ
        entropy từ đơn của tiếng Việt, mô hình \textit{bỏ qua} giai đoạn phải học lại phân
        phối biên và dồn toàn bộ tín hiệu gradient vào việc học ngữ nghĩa.
  \item \textbf{Khác 0 -- để giữ ngữ nghĩa}: phép nhân vô hướng bảo toàn \textit{hướng} của
        các hàng $W_{\mathrm{dec}}$, tức bảo toàn cấu trúc lân cận ngữ nghĩa thu được từ phép
        chiếu SALT. Trong quá trình huấn luyện, mô hình chỉ cần \textit{khuếch đại dần} biên
        độ của tín hiệu ngữ nghĩa sẵn có thay vì học lại từ đầu, nhờ đó tốc độ học về sau
        nhanh hơn so với một lớp đầu ra ngẫu nhiên.
\end{itemize}

Giả thuyết này được kiểm chứng bằng thực nghiệm trong Chương~\ref{chap:experiment}: khi giữ
nguyên biên độ ($\gamma = 1$) hoặc chỉ co một nửa ($\gamma = 0{,}5$), mất mát khởi điểm cao
hơn hẳn (với $\gamma=1$ gần chạm mức ngẫu nhiên $\log|\mathcal{V}_t|$, với $\gamma=0{,}5$ vẫn
cao hơn nhiều mức entropy từ đơn) và -- quan trọng hơn -- quá trình huấn
luyện bị \textit{mắc kẹt} ở mức mất mát cao, không hội tụ về vùng của các phương án tốt.
Hiện tượng này không chỉ là hệ quả của điểm khởi đầu cao: một mô hình khởi tạo ngẫu nhiên
chuẩn cũng bắt đầu gần $\log|\mathcal{V}_t|$ nhưng vẫn hội tụ bình thường. Dữ liệu thực
nghiệm (Chương~\ref{chap:experiment}) xác nhận một phần cơ chế này: tại bước 10, chuẩn
gradient (\texttt{grad\_norm}) của $\gamma=1{,}0$ đạt ${\approx}68$ -- cao hơn 33 lần so với
${\approx}2$ của $\gamma=0{,}1$ -- trong khi ở bước đó logits chủ yếu vẫn là nhiễu vì
$W_{\mathrm{dec}}$ chưa hề được căn chỉnh trên tiếng Việt. Điều này xác nhận các gradient
ngược xuất hiện sớm ở $\gamma=1$ có biên độ rất lớn nhưng mang ít thông tin ngữ nghĩa. Một
cách lý giải là các gradient đó làm biến dạng cấu trúc lân cận ngữ nghĩa mà SALT đã xây dựng
trong $W_{\mathrm{dec}}$, phá hủy tài sản chuyển giao trước khi nó kịp phát huy hiệu quả;
phần này vẫn cần kiểm chứng trực tiếp, ví dụ bằng cách đo độ dịch chuyển tương đồng cosine
của các hàng $W_{\mathrm{dec}}$ qua từng bước huấn luyện đầu.
Trong khi đó, $\gamma = 0{,}1$ vừa cho khởi điểm thấp vừa hội tụ nhanh. Do đó $\gamma = 0{,}1$
được chọn cho toàn bộ quy trình.

% ======================================================================
\section{Giai đoạn căn chỉnh đóng băng}
\label{sec:freeze}

Sau bước khởi tạo, ma trận nhúng và ma trận đầu ra của tiếng Việt là \textit{mới} đối với
phần thân Transformer đã được huấn luyện trên tiếng Anh. Nếu lập tức tinh chỉnh toàn bộ mô
hình, phần thân có thể bị kéo theo để thích ứng với hai ma trận mới còn chưa ổn định, làm gia
tăng nguy cơ quên lãng thảm họa (Mục~\ref{subsec:lang-adapt}). Vì vậy, chúng tôi chèn một
\textit{giai đoạn căn chỉnh đóng băng}: đóng băng toàn bộ phần thân Transformer và chỉ huấn
luyện ma trận nhúng đầu vào, ma trận đầu ra và độ chệch của nó trên một lượng nhỏ ngữ liệu
tiếng Việt với mục tiêu MLM.

Trực giác của bước này là để hai ma trận vừa khởi tạo \textit{căn chỉnh với nhau và với phần
thân cố định} trong không gian biểu diễn trước khi cho phép phần thân thay đổi; nhờ đó điểm
khởi đầu của giai đoạn tiếp tục tiền huấn luyện ổn định hơn. Cách làm này nằm trong tinh thần
chiến lược \textit{đóng băng rồi thích nghi} của de Vries và
Nissim~\cite{devries2021recycle}, vốn cho thấy việc tách giai đoạn học lớp nhúng khỏi giai
đoạn tinh chỉnh toàn bộ giúp tái sử dụng mô hình hiệu quả hơn. Trong thực nghiệm
(Chương~\ref{chap:experiment}), chúng tôi quan sát thấy giai đoạn này \textit{luôn} cải thiện
chất lượng lớp nhúng khởi tạo, do đó được giữ lại trong quy trình.
% ======================================================================
\section{Tiếp tục tiền huấn luyện thích nghi}
\label{sec:cpt}

Ở giai đoạn cuối, toàn bộ mô hình được tinh chỉnh trên ngữ liệu tiếng Việt quy mô lớn
CulturaX với mục tiêu MLM. Theo Gururangan và cộng sự~\cite{gururangan2020dapt}, việc tiếp
tục tiền huấn luyện trên dữ liệu miền đích mang lại cải thiện ổn định; ở đây miền đích là
toàn bộ ngôn ngữ tiếng Việt.

\textbf{Lịch tốc độ học WSD.} Chúng tôi sử dụng lịch \textit{Warmup--Stable--Decay}
(WSD)~\cite{hu2024minicpm} thay cho lịch cosine cố định, vì WSD tách pha ổn định khỏi pha
làm nguội nên cho phép kéo dài huấn luyện tùy ý mà không phải cam kết trước tổng số bước.
Tốc độ học $\eta$ theo bước $s$ được định nghĩa như Công thức~\eqref{eq:wsd}:
\begin{equation}
  \label{eq:wsd}
  \eta(s) =
  \begin{cases}
    \eta_{\mathrm{peak}} \cdot \dfrac{s}{S_w} & \text{nếu } s < S_w \quad \text{(khởi động)}\\[2mm]
    \eta_{\mathrm{peak}} & \text{nếu } S_w \le s < S_c \quad \text{(ổn định)}\\[2mm]
    \eta_{\mathrm{peak}} \cdot \Big(1 - \sqrt{\tfrac{\,s - S_c\,}{S_d}}\,\Big) & \text{nếu } s \ge S_c \quad \text{(làm nguội)}
  \end{cases}
\end{equation}
Trong đó $S_w$ là mốc kết thúc khởi động, $S_c$ là mốc bắt đầu làm nguội, và $S_d$ là độ dài
giai đoạn làm nguội. Pha \textit{ổn định} giữ tốc độ học ở đỉnh trong phần lớn quá trình,
còn pha \textit{làm nguội} chỉ chiếm một phần nhỏ cuối ngân sách và dùng dạng căn bậc hai
$1-\sqrt{f}$ (với $f\in[0,1]$ là phần bước đã trôi qua trong pha làm nguội) -- dạng mà Hägele
và cộng sự~\cite{hagele2024scaling} chứng minh cho chất lượng hạ nguồn tốt nhất ở một ngân
sách cho trước. Hình~\ref{fig:wsd} minh họa hình dạng của
lịch và ưu điểm cốt lõi của nó: tại bất kỳ điểm nào trong pha ổn định đều có thể ``rẽ
nhánh'' một pha làm nguội ngắn để thu về mô hình hoàn chỉnh, trong khi nhánh chính tiếp tục
huấn luyện -- điều mà lịch cosine (phải cam kết trước tổng số bước) không làm được.

\begin{figure}[ht]
  \centering
  \begin{tikzpicture}[>={Latex[length=2mm]}, scale=1.0]
    % axes
    \draw[->] (0,0) -- (11.3,0) node[right, font=\small] {$s$};
    \draw[->] (0,0) -- (0,3.2) node[left, font=\small] {$\eta(s)$};
    % warmup
    \draw[very thick, blue!70] (0,0) -- (1.5,2.4);
    % stable
    \draw[very thick, blue!70] (1.5,2.4) -- (8.2,2.4);
    % decay 1-sqrt (main branch at the end)
    \draw[very thick, red!75] plot[smooth] coordinates
      {(8.2,2.4) (8.5,1.62) (8.9,1.17) (9.4,0.78) (9.9,0.46) (10.4,0.18) (10.7,0)};
    % mid-run cooldown branch (milestone)
    \draw[thick, red!60, dashed] plot[smooth] coordinates
      {(4.6,2.4) (4.85,1.62) (5.15,1.17) (5.5,0.78) (5.85,0.46) (6.2,0.18) (6.4,0)};
    % peak dashed line
    \draw[dashed, gray] (0,2.4) -- (1.5,2.4);
    \node[left, font=\small] at (0,2.4) {$\eta_{\mathrm{peak}}$};
    % milestones marks
    \draw[dashed, gray] (1.5,0) -- (1.5,2.4);
    \node[below, font=\small] at (1.5,0) {$S_w$};
    \draw[dashed, gray] (8.2,0) -- (8.2,2.4);
    \node[below, font=\small] at (8.2,0) {$S_c$};
    \node[below, font=\small] at (10.7,0) {$S_c + S_d$};
    % phase labels
    \node[font=\small, blue!70] at (0.6,1.6) {warmup};
    \node[font=\small, blue!70] at (4.9,2.72) {stable};
    \node[font=\small, red!75] at (10.3,1.5) {decay};
    \node[font=\small, red!60] at (6.5,1.5) {mid-run branch};
  \end{tikzpicture}
  \caption{Lịch tốc độ học Warmup--Stable--Decay (WSD).}
  \label{fig:wsd}
\end{figure}

\textbf{Khởi động lại tốc độ học.} Một điểm kiểm tra đã được làm nguội về tốc độ học gần 0
gần như không thể cập nhật trọng số nếu huấn luyện tiếp mà không xử lý. Do đó, mỗi khi tiếp
tục huấn luyện trên dữ liệu mới (ví dụ một pha làm nguội trên dữ liệu mới), tốc độ học được
\textit{khởi động lại} rồi làm nguội lại theo đúng khuyến nghị của Ibrahim và cộng
sự~\cite{ibrahim2024continual}. Ngoài ra, khi huấn luyện tiếp, trạng thái bộ tối ưu AdamW
(các mô-men bậc nhất và bậc hai, với $\beta = (0{,}9;\, 0{,}95)$ theo NeoBERT) được nạp lại
\textit{nguyên vẹn}, để bộ tiền điều kiện của bộ tối ưu khớp với chế độ mà phần thân đã
được huấn luyện.

% ======================================================================
\section{Tổng kết phương pháp}
\label{sec:method-summary}

Tóm lại, phương pháp xây dựng ViNeoBERT gồm ba giai đoạn: (1)~khởi tạo lớp nhúng và lớp đầu
ra bằng phép chiếu SALT theo từng token từ ViDeBERTa sang không gian NeoBERT, với tập neo
được làm giàu bằng cặp dịch và độ chệch đầu ra khởi tạo theo tần suất; (2)~căn chỉnh đóng
băng để hai ma trận mới ổn định cùng phần thân; và (3)~tiếp tục tiền huấn luyện thích nghi
theo lịch WSD. So với SALT gốc, đề tài có ba khác biệt chính: \textit{mở rộng tập neo bằng
cặp dịch} (Mục~\ref{sec:anchor}), \textit{áp dụng phép chiếu cho cả lớp đầu ra tách rời --
kèm khởi tạo độ chệch theo tần suất và co trọng số đầu ra để tiên nghiệm dẫn dắt giai đoạn
đầu} (Mục~\ref{sec:salt-proj}), và \textit{bổ sung giai đoạn căn chỉnh đóng băng}
(Mục~\ref{sec:freeze}). Hiệu quả của các thành phần này được đánh giá trong
Chương~\ref{chap:experiment}.
